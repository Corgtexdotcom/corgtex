---
title: "Branching and Pull Requests"
description: "How PRs flow through the autonomous Planner → Executor → Reviewer pipeline."
---

Corgtex uses an autonomous three-agent pipeline for all development.
The full specification is in [Agent Pipeline](/contributing/agent-pipeline).
This page covers the PR-specific conventions that apply to every change.

## Branching

- One branch per plan. No overlapping scopes.
- Descriptive names: `feat/optimistic-finance`, `fix/login-crash`, `docs/agent-pipeline`.
- Branch name becomes the plan filename: `docs/plans/<branch>.md` (lowercased, `/` replaced by `-`).

## The plan is the contract

Every PR is backed by a plan file at `docs/plans/<branch>.md`. The plan
is the handoff between Planner, Executor, and Reviewer, and it is
enforced mechanically:

- `plan-present` CI job fails if the plan file is missing.
- `scope-check` CI job fails if any changed file is outside the plan's "Files to touch" allowlist, except visual proof under `docs/assets/<branch-slug>/`.
- The PR body must link to the plan file and state the risk tier.
- The Reviewer (Codex) rejects PRs whose plan lacks ticked acceptance criteria.

Copy [`docs/plans/_TEMPLATE.md`](https://github.com/corgtex/corgtex/blob/main/docs/plans/_TEMPLATE.md) to start.

## Opening the PR

The Executor opens the PR using `gh pr create` (installed via
Homebrew; `/opt/homebrew/bin/gh` if not on `PATH`). The PR body:

1. **Links to the plan file.**
2. **States the risk tier** — `low`, `standard`, or `high`.
3. **Summarizes what shipped** — one or two sentences, the "walkthrough."
4. **Links committed visual proof** under `docs/assets/<branch-slug>/` for any change under `apps/web/app/**`, `apps/web/components/**`, or `apps/web/lib/components/**`.

Auto-merge is set by the Executor immediately (`gh pr merge --auto --squash`) so the PR merges as soon as the Reviewer approves and required checks are green.

## CI gates

Every PR must pass:

- `check` — lint, typecheck, Prisma validate.
- `test:unit` / `test:integration` / `test:all` — Vitest unit and DB-backed integration coverage. `test:integration` uses `docker-compose.test.yml`.
- `db-sync` — migration ↔ schema parity.
- `build` — `env -u DATABASE_URL npm run build` (database-independent).
- `docs` — `mint validate` and broken-link check.
- `plan-present` — plan file exists for the branch.
- `scope-check` — changed files ⊆ plan allowlist, validates policy checks, and allows branch visual proof.
- `gitleaks` — no secrets in the diff.
- `diff-size` — risk-tier caps: `low` ≤ 1200 non-doc LOC / 50 files, `standard` ≤ 800 / 25, `high` ≤ 400 / 15.

All of these are required by branch protection on `main`. The Reviewer
cannot merge a PR whose required checks are red.

## Labels

- `forbidden-path-approved` — justification in the plan, required for changes to `deploy/**`, `.github/workflows/**`, `prisma/migrations/**`, `packages/domain/src/auth*.ts`, `apps/web/lib/auth.ts`.
- `large-change-approved` — override for the selected risk-tier cap.
- `halt-agents` — human stop; Reviewer will not merge, Executor will not push.
- `force-merge` — human override, logged.
- `needs-replan` — Executor is stuck; Planner picks up.
- `auto-revert` — created by `.github/workflows/auto-revert.yml` after a failed `smoke-prod`.

## Automated Document Updates

When a PR merges to `main`, our Mintlify AI workflow evaluates the diff
to determine if documentation is now outdated. If so, it drafts a
separate PR against the docs site through the same Planner → Executor →
Reviewer pipeline described above.
